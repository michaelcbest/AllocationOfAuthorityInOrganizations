* 1. Poor performance on procurement and career prospects (Figure A2)
do "${code}/8-1_FigureA2.do"

* 2. Reasons for lack of value for money (Figure A3)
do "${code}/8-2_FigureA3.do"

* 3. Budget release unaffected (figure A4)
do "${code}/8-3_FigureA4.do"

* 4. Figure A.6
do "${code}/8-4_FigureA6.do"

* 5. Figure A7
do "${code}/8-5_FigureA7.do"

* 6. Balance of attrition (Figure A8 and table A3)
do "${code}/8-6-1_DataBuild.do"
do "${code}/8-6-2_TableA3.do"
do "${code}/8-6-3_FigureA8.do"

* 7. Figure A10
do "${code}/8-7_FigureA10.do"

* 8. Dice scores in year 1 (Figure A11)
do "${code}/8-8_FigureA11.do"

* 9. Time allocation across procurement tasks (figure A12)
do "${code}/8-9_FigureA12.do"

* 10. Heterogeneity by PO dice score (table A4)
do "${code}/8-10_TableA4.do"
